# Second Railway service (Web): paste the uvicorn line below into Settings → Deploy → Start Command.
# Same env as the worker (especially DATABASE_URL). Railway sets PORT automatically.
web: uvicorn dashboard.app:app --host 0.0.0.0 --port ${PORT:-8080}
